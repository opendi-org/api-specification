{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "DI-Causal-Type-v0.0-alpha-0325",
    "title": "Causal Type",
    "description": "The causal type of the decision element.",
    "$comment": "Ensures causal type names are consistent wherever used.",
    "oneOf": [
        {
            "enum": ["Lever", "Intermediate", "Outcome", "External", "Goal", null]
        },
        {
            "type": "string",  "pattern": "^CUSTOM_.*"
        }
    ]
}